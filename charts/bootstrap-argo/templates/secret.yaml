apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.source.secretName }}
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: {{ .Values.source.repoUrl }}
  username: {{ .Values.source.username }}
  password: {{ .Values.source.password }}
