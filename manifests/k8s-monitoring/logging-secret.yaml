apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-cloud-logs-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: cluster-secrets
  target:
    name: grafana-cloud-logs-credentials
  data:
    - secretKey: username
      remoteRef:
        key: grafana-credentials
        property: GCLOUD_HOSTED_LOGS_ID
    - secretKey: password
      remoteRef:
        key: grafana-credentials
        property: GCLOUD_K8S_RW_TOKEN
