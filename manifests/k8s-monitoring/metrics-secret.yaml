apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-cloud-metrics-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infra-secrets
  target:
    name: grafana-cloud-metrics-credentials
  data:
    - secretKey: username
      remoteRef:
        key: GCLOUD_HOSTED_METRICS_ID
    - secretKey: password
      remoteRef:
        key: GCLOUD_API_TOKEN
