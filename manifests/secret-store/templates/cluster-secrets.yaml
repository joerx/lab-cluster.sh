{{- if eq .Values.backend "infisical" }}
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: cluster-secrets
spec:
  provider:
    infisical:
      hostAPI: '{{ .Values.infisical.endpoint }}'
      auth:
        universalAuthCredentials:
          clientId:
            key: clientId
            namespace: external-secrets
            name: universal-auth-credentials
          clientSecret:
            key: clientSecret
            namespace: external-secrets
            name: universal-auth-credentials
      secretsScope:
        projectSlug: '{{ .Values.infisical.project }}'
        environmentSlug: '{{ .Values.infisical.environment }}'
        secretsPath: '{{ .Values.infisical.path }}'
{{- else if eq .Values.backend "kubernetes" }}
# Local backend: reads from the 'cluster-secrets' Secret in the 'local-secrets' namespace.
# ExternalSecrets using this store must set remoteRef.key: cluster-secrets and
# remoteRef.property: <KEY> to select a specific value.
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: cluster-secrets
spec:
  provider:
    kubernetes:
      remoteNamespace: local-secrets
      server:
        url: "https://kubernetes.default.svc"
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          namespace: kube-system
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-local-reader
          namespace: external-secrets
{{- end }}
